<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.UnlockReminder"
        android:supportsRtl="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MessageDialogActivity"
            android:exported="false"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"/>

        <!--
            UnlockReceiver handles BOOT_COMPLETED only — it restarts UnlockMonitorService
            after reboot so the dynamically registered ACTION_USER_PRESENT receiver is
            re-registered. ACTION_USER_PRESENT is handled inside UnlockMonitorService.
        -->
        <receiver
            android:name=".receiver.UnlockReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!--
            UnlockMonitorService keeps the app process resident (Android 14+ Cached App Freezer)
            AND dynamically registers the ACTION_USER_PRESENT receiver so unlocks are reliably
            detected without relying on manifest-registered receivers in a frozen process.
        -->
        <service
            android:name=".service.UnlockMonitorService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps the process resident so dynamically registered ACTION_USER_PRESENT receiver fires reliably on Android 14+ Cached App Freezer."/>
        </service>

    </application>
</manifest>
